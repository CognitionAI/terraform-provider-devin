---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "devin_org_group_limits Resource - devin"
subcategory: ""
description: |-
  Manages the enterprise's org-group ACU limit configuration. This is a singleton: only one should be declared per enterprise, and applying it replaces the entire set of groups. Each org may belong to only one group.
---

# devin_org_group_limits (Resource)

Manages the enterprise's org-group ACU limit configuration. This is a singleton: only one should be declared per enterprise, and applying it replaces the entire set of groups. Each org may belong to only one group.

!> **Singleton** — declare at most one `devin_org_group_limits` per enterprise. Each apply replaces the entire org-group configuration, so a second declaration (or out-of-band edits) will be overwritten on the next apply. Each organization may belong to only one group.

## Example Usage

```terraform
# Enterprise singleton: declare at most one, owning the whole group config.
resource "devin_org_group_limits" "all" {
  groups = [
    {
      name           = "product-teams"
      org_ids        = [devin_organization.frontend.org_id, devin_organization.backend.org_id]
      max_cycle_acus = 5000
    },
    {
      name    = "experiments"
      org_ids = [devin_organization.labs.org_id]
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `groups` (Attributes Set) Org groups and their limits. Replacing this set replaces the whole configuration. (see [below for nested schema](#nestedatt--groups))

<a id="nestedatt--groups"></a>
### Nested Schema for `groups`

Required:

- `name` (String) Group name.
- `org_ids` (Set of String) Organization IDs in this group. Each org may appear in only one group.

Optional:

- `max_cycle_acus` (Number) Maximum ACUs per cycle shared across the group. Omit for no limit.
