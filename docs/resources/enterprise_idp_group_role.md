---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "devin_enterprise_idp_group_role Resource - devin"
subcategory: ""
description: |-
  Maps a registered IdP group to an enterprise-wide (account-level) role. Members of the IdP group inherit the assigned role across the enterprise. Use devin_org_idp_group_role for organization-scoped roles.
---

# devin_enterprise_idp_group_role (Resource)

Maps a registered IdP group to an enterprise-wide (account-level) role. Members of the IdP group inherit the assigned role across the enterprise. Use devin_org_idp_group_role for organization-scoped roles.

## Example Usage

```terraform
# Look up role IDs instead of hard-coding them.
data "devin_roles" "all" {}

locals {
  enterprise_admin_role_id = one([
    for r in data.devin_roles.all.roles : r.role_id
    if r.role_type == "enterprise" && r.role_name == "Admin"
  ])
}

# Grant an enterprise-wide role to everyone in the IdP group.
resource "devin_enterprise_idp_group_role" "platform_admins" {
  idp_group_name = devin_idp_group.platform.name
  role_id        = local.enterprise_admin_role_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `idp_group_name` (String) Name of a registered IdP group (see devin_idp_group).
- `role_id` (String) Role to assign. Must be an enterprise (account-level) role.

## Import

Import is supported using the following syntax:

```shell
# Enterprise IdP group roles are imported by the IdP group name.
terraform import devin_enterprise_idp_group_role.platform_admins platform
```
